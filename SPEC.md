# なみラボ (Nami Lab) — 波のシミュレーション 仕様書 v1（合意済・実装中 2026-09-23）

高校1年 物理基礎「波」（音を除く・素元波系は応用）の授業用Webアプリ。
教師のプロジェクター投影が主用途。生徒が手元（Chromebook／スマホ）で見返し・操作する副用途。
クイズ・答え合わせは内蔵しない（問いは授業と問題集の役目）。「見せる計器」に徹する。

### 設計原則（最上位。個別仕様より優先）
- **生徒に思考させる道具である。** 授業は創意工夫をし思考力を鍛える場で、「問題を解くための盲目的なテクニック」を教える場ではない。生徒が「どこがどうなるからこの形になる」と自分の言葉で説明できるようになるための材料を画面に置く。
- したがって **生徒を思考停止させる文・アドバイス・機能は入れない**: 結論だけの自動判定（「→ 強め合い」）、暗記則（「○○なら××」）、理由抜きの手品的操作（鏡像トグル等）、「こう覚える」式の注記。
- 手順（作図法・横波表示の矢印回転など）を出すときは、**同じ画面にその手順が成り立つ理由**を置く。結論を出すなら、根拠が画面で読める形（例: 干渉の点 P の y-t グラフで振幅 2A／0 を見せる）にする。
- 個別の禁止項目を列挙して守るのではなく、機能・文言ごとに「これは生徒の思考を促すか、止めるか」で判断する。

合意日: 2026-09-23（会話ベース）／教科書: 数研出版（用語「定在波」・縦波の符号は右向き正）

---

## 0. 全体構成

### ハブ
- 教育用アプリのハブを専用リポジトリに置く（github.io直下は使わない＝非教育アプリの余地を残す）。
  - `scicos-lab` → `https://torao-cos.github.io/scicos-lab/`（確定 2026-09-23）
  - 掲載: おとラボ（既存）／なみラボ（本アプリ）／元素記号・化学式カード（既存）／後日「おと」シミュ
- なみラボ自体は `https://torao-cos.github.io/nami-lab/`。各ページ上部にハブへ戻るリンク。

### ページとタブ（大項目＝ページ＝描画エンジン単位／類似項目＝ページ内タブ／番号＝owner原案＝授業順）
| ページ | ファイル | タブ |
|---|---|---|
| A 波の基本 | `basic.html` | 1 波面と射線／2 単振動と正弦波／3 波の5量 |
| B グラフの読み方 | `graph.html` | 4 y-x⇄y-t／5 媒質の速度 |
| C 縦波 | `longitudinal.html` | 6 縦波と横波／7 地震P波S波／8 縦波の横波表示 |
| D 重ね合わせ | `superpose.html` | 9 重ね合わせ／10 反射／11 定在波／12 縦波の定在波 |
| E 応用（ホイヘンス） | `applied.html` | 13 素元波／14 反射（隊列モデル＋素元波の作図で入射角＝反射角。詳しくで壁つき波動タンク）／15 屈折（左右2画面: 隊列モデル＋二媒質の波動タンク。λ・v・T を同調）／16 屈折の作図と全反射／17 回折／18 干渉 |

（2026-09-24 第3弾: 反射タブ新設・屈折は左右2画面でタンクの振動数を隊列に同調・地震は操作子を右列へ、時間バーを地震発生からの時間・S波が通り抜けたらリピート、P/S 各「射線・波面・影」を別チェック、再生速度 ×15〜×600。共通ライブラリに `ctx.layout('side')`、api フック `loop()/tMax()/timeLabel()/canStepBack` を追加。）

（番号は 2026-09-24 owner 指示で振り直し。本文中の見出し D-R→10、D-10→11、D-11→12、E-12→13、E-12b→14、E-12c→15、E-I→16 と読み替える。節・腹のマーカーは記号○▲でなく文字「節」「腹」。パルスは完全に通り過ぎたら自動リピート。書体: 変数＝斜体・単位＝立体（common.js が自動適用）。）

- URLハッシュでタブを直接指定できる（例 `superpose.html#reflect`）。スライド・QRから当該タブへ直行。
- `index.html` はなみラボのTOP（5ページのカード）。ハブからはここへリンク。

### 技術
- 外部ライブラリ・外部通信・ビルドなし。GitHub Pages（HTTPS）。MIT License／© SciCos。
- ページ間共通の `common.css` / `common.js`（同一リポジトリ内・CDN不使用）。各ページは共通ファイル＋自身のみで動く。
- Canvas 2D。描画は requestAnimationFrame。数値は物理時間で進め、フレーム落ちしても物理が狂わない（dtは実時間×速度倍率、上限あり）。
- UIは日本語・ダークテーマ（おとラボと同じパレット・同じフォント）。
- レイアウト優先: ①PC横長（投影）②Chromebook ③スマホ縦持ち（パネルは縦積み・横スクロール禁止）。
- 可読性は機能の制約にしない（投影で細字が読めなくても口頭で補う）。操作子の数は制限しない。

---

## 1. 全ページ共通仕様

### 時間制御
- **キャンバスのクリック／タップ＝停止⇄再開**（ドラッグと区別: ポインタ移動5px未満をクリック扱い）。Spaceキーも同じ。
- **コマ送り** ◀ ▶（1コマ＝T/12。←→キー）。停止中のみ有効。
- **スロー** ×1／×1/2／×1/4。
- **時間スクラブ**: 停止中、時間バーをドラッグして任意の時刻へ（過去へも戻れる。全タブとも状態は t の関数で再計算できる設計にする。回折タブのみ例外→§E）。
- **リセット**（t=0・パラメータは保持）。
- 停止中も注目点・カット線・波源などのドラッグ操作は可能（既存教材との最大の差別化点）。

### 「詳しく」トグル
- 各タブに1つ。既定画面は教科書準拠の最小表示。発展・厳密寄りの表示はここに隠す（例: 8の反転ボタン、1の減衰）。

### 表示トグル
- 要素ごとに表示／非表示（速度矢印・入射波・反射波・合成波・射線・包絡線 等）。発問時に隠して見せる用途。

### 単位・既定値
- 実数表示: λ[m]・T[s]・v[m/s]・A[m]・f[Hz]。既定 λ=2.0 m, T=2.0 s（v=1.0 m/s）, A=1.0 m。
- 進行波の式（右進行）: y = A sin 2π(t/T − x/λ)。左進行は −x を +x に。
- 目盛り付き軸を常時表示（x は m、t は s）。

### 色（全ページ固定）
| 要素 | 色 |
|---|---|
| 変位・波形（合成波） | 緑 `#54ff8a`（おとラボの波形色） |
| 入射波・波1 | 青 `#69b7ff`（破線） |
| 反射波・波2 | 赤 `#ff6b6b`（破線） |
| 媒質の速度 | 橙 `#ffa64d` |
| 疎密（圧力） | 紫 `#c792ff` |
| 節 | 黄 `#ffd166` ○ ／ 腹 | 水色 `#4dd0e1` ▲ |
| 射線・作図補助線 | 灰 `#9ac5ad` |
| 仮想領域（壁の向こう等） | 同色を不透明度30% |

### 端末・入力
- viewport は おとラボと同じ（`user-scalable=no`）。キャンバス `touch-action:none`。
- マウス／タッチ両対応。ドラッグ対象はヒット半径を大きめ（指用）。

---

## 2. 各タブ仕様

### A-1 波面と射線
- **既定4画面同時**: 上段＝平面波、下段＝球面波。各段 左＝上から見た水面（波面）、右＝カット線に沿った y-x グラフ。
- 各パネル見出しクリックで1面最大化（スマホ用）。
- 左画面: 山を明るい線（球面波はリング）で描き時間で進む。**射線** チェックで波面に直交する矢印を重ねる。
- 左画面に**カット線（x軸）**を描き、右グラフはその線上の y。球面波はカット線（射線）をドラッグで回転でき、どの方向でも同じグラフ＝等方性が見える。
- 球面波の振幅は既定で一定（教科書図）。「詳しく」に減衰（1/√r）トグル。
- 調整: λ・T・A。

### A-2 単振動と正弦波
- 左: 等速円運動する点（半径＝A・周期＝T）と、その鉛直射影。位相角 θ=2πt/T を表示。
- 右: y-t グラフ。**t 軸は右向きに固定し、ペンが左から右へ進む。右端に達したら次のページ（[k·span, (k+1)·span]）へ送る**（2026-09-24 変更。旧「紙が左へ流れる記録計方式」は波形が左へ流れて t 軸が左向きに見えるため廃止）。射影点とペンは同じ高さ（補助線）。この先の y-t（未来）は既定で描かず「詳しく」トグル。
- 調整: 円の大きさ（A）・回転速度（T または f）。
- 「詳しく」: 射影点を A-3 の媒質1粒子に紐づける表示（円運動＝波の中の1点の運動）。

### A-3 波の5量（λ・A・T・f・v）
- 右進行波の y-x。λ ブラケット（山→山）・A ブラケット（軸→山）を常時注記。
- 固定点 P（ドラッグ可）の y-t を小パネルで記録計表示し、T ブラケット。
- **定規**（画面上をドラッグ可・m目盛り）と**ストップウォッチ**（停止・再開と連動）を置き、自分で測れる。
- **「1周期だけ進める」ボタン**: 時間を正確に T だけ進め、山が λ だけ動いたことを残像（開始時の波形を薄く残す）で見せる。
- **山カウンタ**: 固定点 P を通過した山の数を数え、1秒あたりの個数から f を表示。
- 式パネル: `v = fλ`, `f = 1/T`, `v = λ/T` に現在値を代入した形で常時表示。スライダーに同時追従。
- 調整: λ・T・A。

### B-4 y-x ⇄ y-t
- **4グラフ1画面（2列×2段・2026-09-24 owner決定）**
  - 左列＝場所を決めて時間を見る（y-x → y-t）: 左上＝動き続ける y-x（観測点 x₀ をドラッグ）／左下＝x₀ の y-t。**x₀ を置いた・動かした瞬間から記録が始まる**（それ以前の記録は消す。y-t は「その場所を見張っていた記録」だから）。
  - 右列＝時刻を決めて場所を見る（y-t → y-x）: 右上＝場所 x₁ の y-t を全体まで描いた静止画に時刻カーソル t₁（ドラッグ。再生中も手で選んだ時刻だけが変わる）／右下＝時刻 t₁ の y-x（波の写真）。x₁ の位置に印を付け、上で読んだ変位と同じ高さであることが補助線で見える。x₁ は右下でドラッグでき、動かすと右上が描き直される。
  - λ・T・A は左右共通。見出しに役割（左「時間が流れる」／右「時刻を手で選ぶ」）を明記。
- y-t は「その場所の媒質が時間とともにどう動いたか」をカット線のドラッグとコマ送りで生徒自身に考えさせる（§0「設計原則」）。かつて置いた「y-x を左右反転すると y-t になる」トグルは、生徒の思考をそこで止めてしまうので撤去した（2026-09-24）。
- 「詳しく」: 進行方向の反転（左進行波）。

### B-5 媒質の速度
- y-x 上に **全媒質点の速度矢印**（v_y=∂y/∂t。上向き・下向きで色濃淡、山谷は0でドットのみ）。
- **ゴースト波形**: Δt 後（Δt はスライダー、既定 T/12）の波形を半透明で重ねる（紙の指導技の画面化）。
- **4グラフ1画面（2列×2段・2026-09-24 owner決定）**: 左上＝y-x（速度矢印・ゴースト・注目点 P）／左下＝v-x（同じ x 軸）／右上＝P の y-t＋接線／右下＝P の v-t（同じ t 軸）。接線の傾きが真下の v-t の値と一致することが縦に並んで見える（現在時刻の縦線を右列2段に貫通）。
- 速度矢印の長さ＝Δt の間の移動量 v·Δt（矢印の先がゴースト波形の上に乗る）。
- 「y-t の傾きは速度、y-x の傾きは速度ではない」を同一画面で対比。読み出しは「媒質の速さの最大 2πA/T（変位0を通る瞬間）」と「波の速さ v=λ/T（山が進む速さ）」を区別。
- 「詳しく」: 進行方向の反転。

### C-6 縦波と横波
- 同一の玉列（N≈40）を **横波／縦波でトグル**（並置ではなく同じ媒質で切替）。両方同時表示も可。
- 玉を1つ着色し「媒質は進まない」を示す。各玉に速度矢印（トグル）。
- λ ブラケット: 横波は山→山、縦波は密→密。変位ブラケット（つり合い位置からのずれ）。
- 調整: λ・T・A。

### C-7 地震のP波・S波
- 地球断面（半径6371 km／外核境界2891 km深／内核境界5150 km深）。層の色分けと「固体／液体／固体」ラベル。
- 波線（射線）: 震源から放射状に射出し、球対称の速度分布 v(r)（PREM を簡略化した区分線形。マントル・外核・内核で別プロファイル）で **球面のスネルの法則（r·sin i / v = 一定）** により追跡。P（青）と S（赤）を同時表示。S は外核境界で消える。
- 波面: 走時の等値線をアニメーション（P・S を色分け）。
- **シャドーゾーン**: P は 103°〜143°、S は 103° 以遠を地表に帯で表示。計算値が教科書値と数度以内で一致することを受け入れ条件にする（§4）。
- 震源はドラッグ可（深さ含む）。観測点（ドラッグ可）に簡易地震計: P 到達→S 到達の時刻差（初期微動継続時間）を表示。
- 図は「模式」と明記。

### C-8 縦波の横波表示
- 4段: ①玉列（縦波）②各玉の変位矢印（右向き正）③矢印を**90°回転するアニメーション**→横波表示の曲線 ④疎密（圧力）グラフ（紫）。
- 疎・密の位置に自動ラベル（右向き正で、変位が +→− に横切る点＝密、−→+ ＝疎）。「密＝高圧／疎＝低圧」も併記。
- 既定画面に「x軸の正の向き：右」を固定ラベルで明示。
- 「詳しく」: **正の向き反転ボタン**（横波表示が上下反転し、疎密の位置は変わらないことを示す）。
- 調整: λ・T・A。

### D-9 重ね合わせの原理
- 左右から逆向きに進む2つの波。各成分（青・赤破線）と合成（緑実線）。
- 波形選択（ページD共通セレクタ）: パルス＝三角・矩形・のこぎり・ガウス・sin1周期・**自由描画**（キャンバスに描いた形をスムージングしてパルス化）／連続波＝sin・三角・矩形・のこぎり。
- 各波の振幅（符号付き）・波長（幅）・速さを個別設定。**既定はのこぎりパルス**（非対称＝通過後に形が戻る独立性が見える）。
- 「重なった瞬間で停止」ボタン（重心が一致する時刻へジャンプ）。

### D-R 反射（自由端・固定端）
- **上段＝自由端、下段＝固定端**を同じ入射波で同時表示。壁は右端。
- 作図ステップをボタンで1段ずつ重ねる:
  1. 壁を無視して入射波を延長（青破線・壁の向こうは薄く）
  2. 自由端: 反射面で折り返す（線対称）／固定端: 上下反転してから反射面で折り返す（＝反射面の点について点対称）（赤破線）
  3. 入射波＋反射波＝合成波（緑実線）。壁より右は仮想領域として薄表示
- 入射・反射・合成の表示を個別トグル。
- 端の注記: 固定端＝常に変位0（節）、自由端＝変位が最大2A（腹）。
- 波形はページD共通セレクタ（自由描画含む）。連続波を入れると次タブ（定在波・教材2）と同じ絵になる。
- 数値計算モードは置かない（理想の弦では作図法が厳密解）。

### D-10 定在波
- **教材1「2つの進行波の重ね合わせ」**: 左右からの波について速さ・振幅・振動数を**独立に**設定。3つが一致したときだけ定在波。振幅差→節が止まらない不完全な定在波、振動数差→模様が流れる、が見える。
  - 速さの欄に注記「同じ媒質では速さは等しい（シミュレーション上の仮想操作）」。
  - 成分波の表示トグル、包絡線（±2A cos(2πx/λ)。教科書標準の y₁=A sin2π(t/T−x/λ), y₂=A sin2π(t/T+x/λ) の和＝2A sin(2πt/T)cos(2πx/λ)）、節○・腹▲マーカー、節間隔 λ/2 のブラケット。
- **教材2「反射でできる定在波」**: 反射タブと同じエンジンに連続 sin 波を入射。固定端＝端が節／自由端＝端が腹を強調し、端からの節・腹の位置（λ/4 刻み）を表示。
- 長さLの弦の固有振動（共振条件）は**扱わない**（音の単元＝後日の「おと」アプリ）。

### D-11 縦波の定在波
- 4段同一 x 軸: ①玉列（縦波の定在波）②変位の横波表示 ③媒質の速度グラフ（橙）④疎密（圧力）グラフ（紫）。
- 節・腹マーカーを①〜④に貫通表示。注記「変位の節＝疎密の変化が最大／変位の腹＝疎密は変化しない」。
- 生成は教材1方式（左右からの縦波の重ね合わせ）。

### E-12 素元波（ホイヘンスの原理）
- 波面上の点（数は調整可）から素元波の円を描く。**半径 r=vΔt はスライダーで手動**。半径を変えると共通接線（包絡線）＝次の波面が現れる。
- 平面波／球面波の切替。「自動で進める」で Δt ごとに波面を更新するアニメーション。

### E-14 反射（新設 2026-09-24 owner指示）— **左右2画面**
左右でスライダー（入射角 i・壁の角度・λ・T）を共有する。壁は直線で、既定は**垂直な壁に斜め入射 i = 40°**。壁の角度はスライダーか、壁の上の黄色い点のドラッグで変えられる（±40°）。
- **(a) 隊列モデル（現象）**（左）: 波面を「横一列の点の列」として描き、壁へ向かって進む。壁に着いた点から順に**法線方向の速度成分だけを反転**する（壁に沿う成分はそのまま）ので、先に着いた点から折り返して列全体が回転し反射波面になる。連続波（λ 間隔で波面を次々に出す）なので入射波面（青）と反射波面（赤）が交差する。すべて時間 t の関数として計算しており**スクラブ可**。射線・法線・角のトグル。注記は事実の記述のみ。
- **(b) 素元波の作図**（右）: 入射波面の端 A が壁に着いた瞬間から、もう一端 B が壁の B′ に着くまでの時間 Δt に、壁上の各点から出た素元波の半径は「その点に波面が着いてからの経過時間 × v」で点ごとに違う（A のものが最大・B′ のものは 0）。その包絡線が反射波面。A から出た素元波の半径 AA″ = vΔt = BB′ なので、直角三角形 ABB′ と B′A″A は合同 → **i = r**。図に BB′（青）・AA″（赤）・AB′（灰破線）の長さラベル、法線、角 i・r の弧、直角の印。式パネルに `BB′ = v·Δt = ○ m ＝ AA″` → `i = r = ○°` を現在値で表示。**入射角の自動スイープ**（0°→85°→0°）トグル。
- **(c) 「詳しく」**: 右画面を**壁つき波動タンク（FDTD・壁＝変位0の半平面）**に切り替える。反射が現象として出る。タンク表示中は時間を戻せないので ◀ と時間バーを無効化（`api.canStepBack` getter ＋ `sim.stepBack.disabled`）。

### E-15 屈折（現象）— **左右2画面**（2026-09-24 改。旧 E-14）
左右でスライダー（v₁・v₂・入射角 i・T）を共有する。境界はどちらも**同じ高さの水平線**（パネルの縦中央）で、縦方向いっぱいを使う（レターボックスは廃止）。
- **(a) 隊列モデル**（左）: 波面を「横一列に並んで同じ速さで進む点の列」として描く。境界を x = x_c で横切るレイごとに「その点が境界に着く時刻 t_c」を求め、t ＜ t_c は速さ v₁・向き (sin i, −cos i)、t ＞ t_c は速さ v₂・向き (sin r, −cos r) として位置を**時間で追跡**する（作図ではない）。連続波として λ₁ 間隔で波面を次々に出し、境界通過後は λ₂ 間隔になる。射線トグル。注記は事実の記述のみ（「境界に先に着いた点から順に遅くなる」）。
  - 列に沿った点の間隔が境界の前後で変わるのは、同じ本数のレイがつくるビーム幅が cos i : cos r で変わるため（物理的に正しい。必要なら「詳しく」で説明）。
- **(b) 二媒質の波動タンク**（右）: 同じ FDTD を上半分・下半分で速さ（クーラン数）を変えて解く。**波源は「波が入ってくる側の1辺（上の行）に位置に比例した位相を書き込む」斜めの平面波**（画面の端から端まで届く平面波になる）。反射波が境界で自然に出ることも見える。
- **左右の同調（2026-09-24 owner指示）**: 1 m ＝ N セルとして換算する。N は格子から自動で `N = max(ny/(5.5·λ_fast), 16/λ_slow)`（遅い側の λ が 20 セルを下回らない）。速い側のクーラン数を 0.5 に固定し、遅い側は v₁:v₂ の比で決める。波源の周期は `Tst = 2·N·λ_fast` ステップ、計算を進める速さは `Tst/T` ステップ/秒＝**1周期がちょうど T 秒**。隊列モデルの横半幅も `nx/(2N)` に合わせるので、**同じ入射角で左右が同じ縮尺・同じ λ・同じ屈折角の絵**になる。画面に λ₁・λ₂ を m とセルの両方で表示。
- 起動・リセット直後だけ周期の整数倍だけ早送りして画面を波で満たす（位相はずれない）。
- 時間スクラブ不可・**◀ は無効**（`api.canStepBack: false`。回折と同じ扱い）。
- (c) 読み出し: v₁, v₂, λ₁ = v₁T, λ₂ = v₂T, i, r（sin r = (v₂/v₁) sin i）。全反射条件では「屈折波なし」。

### E-12b 屈折 → **16 屈折の作図と全反射**
- 境界（水平）で速さ v₁→v₂。境界上の各点からの素元波は媒質ごとに半径が異なり、包絡線が曲がる作図。
- 入射角 i（波面の傾き）ドラッグ可。
- **作図の途中量を画面に出す（2026-09-24 owner指示）**: 入射波面の端 B が境界 B′ に着くまでの時間 Δt に、B は BB′ = v₁Δt 進み、A から出た素元波は半径 AT = v₂Δt に広がる。図に BB′（青）・AT（緑）・AB′（灰破線）の長さラベル、式パネルに `sin i = BB′/AB′ = v₁Δt/AB′`／`sin r = AT/AB′ = v₂Δt/AB′` →（AB′ が消えて）`sin i / sin r = v₁/v₂ = λ₁/λ₂ = n₁₂` を現在値代入で表示。
- **全反射・臨界角は既定で表示する**（2026-09-24 owner決定。2026-09-24 午前の「詳しくの中だけ」から変更）。臨界角は `sin i_c = v₁/v₂` を現在値で表示し、`v₂ ≤ v₁` のときは「全反射は起きない」と理由つきで出す。注記1行で「発展：全反射は物理基礎の範囲外。4単位の『物理』や中1の光で扱う」。
- **入射角の自動スイープ**トグル: i を 0°→85°→0° と 16 秒周期で連続に往復させ（スライダーも追従）、v₂ ＞ v₁ のとき臨界角で屈折波が消える瞬間を連続で見せる。
- 波面アニメーション（両媒質で λ が変わる）。

### E-12c 回折（2次元波動タンク）→ **17 回折**
- **本ページで唯一の数値計算**: 2次元波動方程式（FDTD、格子 約200×140、吸収境界）。平面波源＋障壁＋スリット。
- スライダーは **スリット幅 w（セル）／波の速さ v（セル/ステップ＝差分法のクーラン数 s = v·dt/dx。0.15〜0.5。安定条件 s ≤ 1/√2 を守る）／波源の周期 T（ステップ）**。**λ = vT は導出量として表示**し、v・T（f = 1/T）・λ を画面に同時表示する（2026-09-24 owner指示。T 固定で v を変えると f は不変のまま λ だけ変わる）。w≫λ で直進、w≲λ で大きく回り込む現象そのものを見せる。
- 単位は「セル」「ステップ」。注記「1セル＝格子の1目盛り。λ とスリット幅 w の比だけが現象を決める」。
- 二重スリットのプリセット（干渉タブとの照合用）。
- 時間スクラブは不可（過去へ戻れない）。停止・再開・スロー・リセット・▶ のみ。**◀（コマ戻し）は無効化**（共通フック `api.canStepBack: false`。← キーでも t を戻さない＝空振りさせない）。Chromebook で 30 fps 以上。
- 素元波の作図（E-12）と並べて「作図＝手順／タンク＝現象」の対応を示す。

### E-I 干渉 → **18 干渉**
- 2点波源 S₁・S₂（間隔 d 調整・同位相／逆位相切替）の上面図。解析式（2つの円形波の和・振幅一定）で描画。
- **強め合い・弱め合いの線**（双曲線 |S₁P−S₂P| = mλ ／ (m+½)λ）を重ねるトグル。
- 任意点 P をドラッグ→ S₁P・S₂P・経路差・λ の何倍かを読み出し。**「強め合い／弱め合い／中間」の自動判定は出さない**（2026-09-24 owner指示: 結論だけの判定はテクニック化。経路差「= ○.○○ λ」で止める）。同位相／逆位相の注記も判定語でなく事実の記述（「逆位相：S₂ は S₁ が山を出す瞬間に谷を出す」）。
- **点 P の y-t グラフ**を並置（PC横長は右列 1/3 幅・スマホ幅は下に縦積み）。`Nami.recorder` で span = 3T・±2A の目安線つき。強め合いの位置は振幅 2A、弱め合いの位置はほぼ 0 になることを判定語なしに読み取らせる（削除した自動判定の代わり）。
- **線の色（ページ内固定）**: 強め合い（大きく動く）＝水色 `#4dd0e1` の実線／弱め合い（ほとんど動かない）＝黄 `#ffd166` の破線。定在波タブの「黄＝節・水色＝腹」と意味を揃える。S₁P・S₂P の補助線は灰（黄・水色は干渉の線に予約）。
- 教科書の作図表示トグル: 山＝実線・谷＝破線の同心円を描き、実線同士／破線同士の交点＝強め合い、実線と破線の交点＝弱め合い。

---

## 3. 扱わないもの（合意済み）
- クイズ・予想→答え合わせ機能
- 長さ有限の弦の固有振動・共振条件（→ 音アプリ）／「厳密版」トグル（非共振時の小振幅定在波の話）
- 反射の数値計算モード
- 音（うなり・弦・気柱・ドップラー）→ 別アプリ。ハブに後日追加

---

## 4. 受け入れ基準
1. Chrome最新（PC）で全ページ・全タブがコンソールエラーなし。
2. 全タブでクリック停止⇄再開・コマ送り・スロー・リセットが動作。回折以外は時間スクラブで過去へ戻れる。
3. スマホ縦持ち（幅 ~400px）で横スクロールが出ない。A-1 の4画面は縦積み＋1面最大化が動作。
4. A-3「1周期だけ進める」で山の移動量が λ と一致（表示値で誤差0）。
5. D-10 教材1で速さ・振幅・振動数を一致させたとき節の変位が常に0（|y|<1e-6）。振幅だけ変えると節の変位が0でなくなる。
6. D-R 固定端で壁の変位が常に0、自由端で壁の変位の最大が 2A。
7. C-7 のシャドーゾーンは PREM 簡略版（速度分布を都合よく調整しない）で計算し、計算値（直接 P の最大角距離は 98° 台になる）と教科書の観測値（103°〜143°）の差の理由（核をかすめて回り込む回折 P は射線の計算に含まれない）を画面に明記する。
8. C-8 の疎密ラベル位置が横波表示の零交差（傾き負＝密）と一致。正の向き反転で横波表示が反転し疎密ラベルは動かない。
9. E-12c が Chromebook 相当（低速CPU）で 30 fps 以上。
10. 全UIが日本語。各タブに URLハッシュで直行できる。

---

## 5. 開発・公開
- Claude（Fable）が設計・実装・検収。ページ単位でサブエージェント並行、共通ライブラリを先に固める。
- 各ページ完成ごとに playwright でスクリーンショット＋自動テスト（受け入れ基準の数値検証を含む）を回し、ページ単位で commit。
- 公開: GitHub 公開リポジトリ `Torao-cos/nami-lab`（MIT・© SciCos）→ GitHub Pages。ハブ用リポジトリは別途。
- 公開後: おとラボにハブへのリンクを追加（1行）。

---

## 6. 確定事項（旧・未決）
- ハブのリポジトリ名 `scicos-lab`（2026-09-23 確定）
- 使用開始＝翌日から。制作順はページA→Eの順、ページ単位で公開（2026-09-23 owner指示「はしから順に」）
